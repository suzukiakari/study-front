import { LinkStyleBoxDirective } from './link-style-box.directive';

describe('LinkStyleBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new LinkStyleBoxDirective();
    expect(directive).toBeTruthy();
  });
});
